config RISCV32
    bool
    select ARM_COMPATIBLE_SEMIHOSTING if TCG
    select DEVICE_TREE # needed by boot.c

config RISCV64
    bool
    select ARM_COMPATIBLE_SEMIHOSTING if TCG
    select DEVICE_TREE # needed by boot.c
